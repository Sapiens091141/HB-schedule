# HB Schedule — Project Context

ระบบจัดการ **ตารางเรียน Sapiens** (สถาบันติว) — เว็บแอปไฟล์เดียว ภาษาไทย
รองรับ **2 คอร์ส: HB และ Skill** ในเว็บเดียว (ดู §1.1)
อัปเดตล่าสุด: 2026-09-02

- **เว็บจริง:** https://sapiens091141.github.io/HB-schedule/
- **Repo:** `Sapiens091141/HB-schedule` (GitHub Pages, branch `main`, root)
- **Deploy:** `git push origin main` → Pages build อัตโนมัติ (มี `.nojekyll` ข้าม Jekyll)
- **gh CLI:** ล็อกอินเป็น `soonlearning09-lab` (เป็น *collaborator* ของ repo)

---

## 1. สถาปัตยกรรม

| ส่วน | รายละเอียด |
|---|---|
| Frontend | **ไฟล์เดียว `index.html`** (HTML+CSS+vanilla JS ล้วน, ไม่มี framework/build), ฟอนต์ Sarabun, เป็น PWA (`manifest.json` + ไอคอน) |
| Backend | **Supabase** (project ref `zpsarnenawaxmhkgjwwg`) ใช้ทั้ง Auth และ Postgres — SDK โหลดจาก CDN, anon key ฝังใน `index.html` (ปลอดภัยด้วย RLS) |
| Hosting | GitHub Pages (static) |

**ค่าคงที่ธุรกิจ:** แสดงปี พ.ศ. (ปี ค.ศ. +543) · วันเรียน/ช่วงเวลาแยกตามคอร์ส ดู §1.1

---

## 1.1 คอร์ส (HB / Skill)

คอนฟิกทั้งหมดอยู่ที่ `const COURSES` ใน `index.html` แหล่งเดียว:

| คอร์ส | วันเรียน (`cdow`) | คาบ (`slots`) |
|---|---|---|
| **HB** | อา/พุธ/ศุกร์/เสาร์ `[0,3,5,6]` | `M10` 10-12 · `M13` 13-15 · `M15` 15-17 · `M17` 17-19 |
| **Skill** | เสาร์/อาทิตย์ `[0,6]` | `S15` 15:00–16:00 |

- **เพิ่มวันเรียน** = เติมเลขวันใน `cdow` · **เพิ่มคาบ** = เติมบรรทัดใน `slots` — **ไม่ต้องแก้ฐานข้อมูล** เพราะ `schedules` เก็บ `iso_date` เป็นวันที่จริง ไม่ได้เก็บวันในสัปดาห์
- **รหัสคาบห้ามซ้ำข้ามคอร์ส** (จึงเป็น `S15` ไม่ใช่ `M15`) เพราะ `SLB`/จานสีใช้ร่วมกันทั้งระบบ (viewer มีลูกคนละคอร์สได้) และตัวตรวจ Import CSV อาศัยรหัสที่ไม่ซ้ำเพื่อจับกรณี import ไฟล์ผิดคอร์ส แล้ว **หยุดพร้อมข้อความบอก** แทนที่จะเงียบๆ สร้างนักเรียนเปล่า
- **คอร์สอยู่ที่ตาราง `students` ตารางเดียว** — `schedules`/`leave_requests`/`lesson_logs` ผูกกับ `student_id` อยู่แล้ว จึงแยกคอร์สตามเจ้าของแถวเอง ไม่ต้องแก้ schema หรือ RLS ของตารางเหล่านั้น
- **คนที่เรียน 2 คอร์ส = 2 แถวใน `students`** คนละ `id` (ตาราง/การลา/บันทึกการเรียน จึงแยกกันสะอาด) · ผู้ปกครองผูกได้ทั้ง 2 id เพราะ `allowed_users.student_ids` เป็น array อยู่แล้ว
- **admin สลับคอร์สที่แถบบน** (จำคอร์สล่าสุดใน `localStorage['hbCourse']`) · `applyCourse()` เขียนทับ `CDOW`/`SLOTS`/`SLOT_SET` (จึงเป็น `let` ไม่ใช่ `const`) ส่วน `SLB` เป็นตารางแปลงรหัสคาบ→เวลา **ของทุกคอร์สรวมกัน** เพราะบันทึกเก่า/การลา/ภาพสรุป ต้องอ่านรหัสคาบข้ามคอร์สได้เสมอ
- **query ทุกตัวกรองด้วย `courseIds()`** (id ของนักเรียนในคอร์สนั้น) ไม่ใช่แค่กรองตอน render — ไม่งั้น feed บันทึกการเรียนที่ `limit 300` จะถูกอีกคอร์สกินโควตาจนคอร์สเล็กไม่เห็นอะไรเลย และ autocomplete ชื่อชีทจะปนข้ามคอร์ส
- **`allStudents` / `allStudentById` = รายชื่อทุกคอร์ส** ใช้ 2 ที่ที่ข้ามคอร์สโดยธรรมชาติ: ผูกนักเรียนกับอีเมล และรายการคำขอลา (ถ้าใช้ `students` ที่กรองไว้ จะผูกนักเรียนอีกคอร์สไม่ได้ และชื่อในคำขอลาจะกลายเป็น `#id`)

**กฎ union — กันข้อมูลเก่าหายเวลาปรับวัน/คาบ:**
`getClassDays()` คืน *วันตามคอนฟิก ∪ วันที่มีข้อมูลจริงในเดือนนั้น* และ `visibleSlots()` คืน *คาบตามคอนฟิก ∪ คาบที่มีข้อมูลจริง* — ถ้าวันหนึ่งคอร์สย้าย/เลิกวันเรียนหรือคาบ ตารางเดือนเก่าจะยังแสดงครบ · **สำคัญที่สุดที่ `buildSchedGrid()`** เพราะ `saveStudent()` ลบตารางทั้งเดือนแล้วเขียนใหม่จากช่องติ๊ก ถ้าวัน/คาบที่มีข้อมูลไม่ถูกวาดออกมา กดบันทึกทีเดียวข้อมูลนั้นหายถาวร

**สีคาบ** มาจาก `SLOT_PALETTE` ไล่ตามลำดับคาบภายในคอร์ส แล้วใส่เป็น inline style — คาบที่เพิ่มใหม่ได้สีเองโดยไม่ต้องเขียน CSS (เดิม hardcode เป็นคลาส `.chip-M10` ฯลฯ ตอนนี้ลบทิ้งแล้ว)

---

## 2. สิทธิ์ผู้ใช้ (Roles)

`allowed_users.role` มี 3 แบบ:
- **`pending`** — สมัครใหม่ รอ admin อนุมัติ (login แล้วเจอหน้า "รอการอนุมัติ")
- **`viewer`** — ผู้ปกครอง/นักเรียน เห็น **หน้ามือถือ (viewer page)** เฉพาะตารางของนักเรียนที่ผูกไว้ (`student_ids`)
- **`admin`** — เข้าถึงทุกอย่าง: จัดการนักเรียน, Import CSV, อนุมัติผู้ใช้, ผูกนักเรียน↔อีเมล, อนุมัติการลา

**การผูกนักเรียน:** `allowed_users.student_ids` (array) → viewer 1 บัญชีผูกได้หลายนักเรียน (เช่นผู้ปกครองมีลูกหลายคน) · admin ผูก/ถอนในแท็บ "จัดการผู้ใช้"

---

## 3. Database schema (Supabase)

| ตาราง | คอลัมน์สำคัญ |
|---|---|
| `students` | `id, name, nick, grade, paid, course` — `course` = `'HB'` \| `'SKILL'` (default `'HB'`) |
| `schedules` | `student_id, iso_date (date), slots (text[])` — unique(student_id, iso_date) |
| `allowed_users` | `email, role, student_ids (int[]), display_name` |
| `leave_requests` | `id, student_id, iso_date, slot, reason, status (pending/approved/rejected), days_notice, requested_by, decided_by, decided_at` — unique(student_id, iso_date, slot) |
| `lesson_logs` | `id, student_id, iso_date, slot, start_time (time), end_time (time), sheets (jsonb), homework, teacher_note, created_by, created_at, updated_by, updated_at` — unique(student_id, iso_date, slot) |

`lesson_logs.sheets` เก็บได้หลายชีทต่อคาบ: `[{name, video_done, pages_done, pages_total}, ...]` (มี CHECK บังคับว่าต้องเป็น array) · `video_done` เป็น boolean ต่อชีท เพราะ **1 ชีท = 1 คลิป**

RLS เปิดบน `leave_requests`: admin เห็น/แก้ได้ทั้งหมด, viewer เข้าถึงเฉพาะของนักเรียนใน `student_ids` ตัวเอง (`= any(u.student_ids)`)

RLS เปิดบน `lesson_logs` เช่นกัน แต่**เข้มกว่า**: อ่านได้เหมือน `leave_requests` (admin ทั้งหมด / viewer เฉพาะ `student_ids`) ส่วน insert/update/delete **จำกัดที่ `role = 'admin'` เท่านั้น** — viewer เป็นผู้อ่านอย่างเดียว

---

## 4. ฟีเจอร์

### 4.1 จัดการตาราง (admin)
- Import CSV จาก Google Sheet (auto-detect เดือน/ปี, merge เข้าเดือนที่เลือก)
- เพิ่ม/แก้/ลบนักเรียน + กรอกตารางรายเดือนแบบ checkbox
- 3 มุมมอง: ตารางรายวัน, สรุปรายเดือนรายคน, heatmap จำนวนต่อช่วง
- Responsive: desktop = ตาราง, mobile = card + bottom nav + FAB

### 4.2 หน้า viewer (มือถือ)
- ผู้ปกครอง/นักเรียนที่ถูกผูก เห็นการ์ดตารางของลูก/ตัวเอง เลื่อนเดือนได้

### 4.3 ขอลา (leave request)
- **viewer แตะคาบเรียน → ขอลา** · กติกา: แจ้ง **≥2 วัน = อนุมัติอัตโนมัติ**, **<2 วัน = รอครูพิจารณา (pending)**, คาบที่ผ่านแล้ว = ลาไม่ได้
- **admin อนุมัติ/ปฏิเสธ** ในกล่อง "📋 คำขอลา" (แท็บจัดการผู้ใช้) + มีจุดแดงเตือน
- แสดงผล: คาบที่ลาอนุมัติ = **ขีดฆ่า + ป้าย "ลา"**, รออนุมัติ = "รอลา" · heatmap ไม่นับคนที่ลาอนุมัติแล้ว
- **ยกเลิกการลา:** viewer แตะคาบที่ลาแล้วซ้ำ → ปุ่ม "ยกเลิกการลา" · ยกเลิกเองได้ถ้าเหลือ **≥2 วัน**, <2 วัน ต้องแจ้งครู

### 4.4 ลงทะเบียนต้องตั้งชื่อผู้ใช้
- ฟอร์มสมัครบังคับกรอก **ชื่อผู้ใช้/ชื่อจริง** (`display_name`) → admin เห็นตอนอนุมัติ · ชื่อ escape กัน XSS

### 4.7 บันทึกการเรียน (lesson log)
- เก็บ **รายคาบ** — 1 แถว = (นักเรียน, วันที่, ช่วงเวลา) → มีประวัติครบ และคำนวณ "ล่าสุดที่เรียน" ของแต่ละคนได้
- **สิ่งที่บันทึกต่อคาบ:** เวลาเข้า-เลิกเรียน · ชีทได้หลายชีท (ชื่อชีท + ติ๊ก "ดูวิดิโอจบแล้ว" + โจทย์ ทำได้/ทั้งหมด กี่หน้า) · การบ้าน · ความเห็นจากผู้สอน
- **หน่วยที่ใช้:** ชีท = **ชีท** · วิดิโอ = **คลิป** · โจทย์ = **หน้า** — สถาบันกำหนดว่า **1 ชีท = 1 คลิปวิดิโอ** จำนวนคลิปที่ดูจบจึงนับจากชีทที่ติ๊กช่องวิดิโอ (`sheets[].video_done`) ไม่มีคอลัมน์นับคลิปแยก
- **การนับสถิติยุบตามชื่อชีท** (`sheetsByName()`) — ชีทเดียวกันเรียนได้หลายคาบ วิดิโอกับโจทย์ใช้กฎคนละแบบ:
  - **วิดิโอ = เคยติ๊กไหม (OR ทุกคาบ)** ไม่ใช่สถานะคาบล่าสุด · workflow จริงของครูคือคาบแรกที่เจอชีทจะดูวิดิโอแล้วติ๊ก คาบถัดๆ ไปทำแต่โจทย์จึงไม่ติ๊ก ถ้าเอาคาบล่าสุดการติ๊กจะถูกลบล้างทุกครั้งที่เรียนชีทเดิมซ้ำ ("ดูวิดิโอจบแล้ว" ย้อนกลับไม่ได้)
  - **โจทย์ = ค่าล่าสุดที่ไม่ว่าง** เพราะเป็นความคืบหน้าสะสมของชีทนั้น (4/8 → 8/8) ถ้าบวกทุกคาบจะได้ 12/16 ทั้งที่จริงคือ 8/8
  - "เข้าเรียนกี่ครั้ง" กับ "รวมชั่วโมง" ยังรวมทุกคาบตามเดิม
- **ป้ายวิดิโอรายคาบแสดงเฉพาะคาบที่ติ๊กจริง** (`✔ ดูวิดิโอจบ` สีเขียว) — คาบที่ไม่ติ๊กแปลว่า "วันนั้นไม่ได้ดูวิดิโอ" ไม่ใช่ "ยังดูไม่จบ" จึงไม่ขึ้นป้ายอะไรเลย · สถานะค้างจริงดูที่สรุปรายเดือน
- **ชื่อชีทมี autocomplete** — ช่องชื่อชีทผูกกับ `<datalist id="sheetNameList">` (ของเบราว์เซอร์เอง ไม่เขียน dropdown เอง) เติมจากชื่อที่เคยใช้ เรียงชีทที่เพิ่งใช้ไว้บนสุด · จำเป็นเพราะสถิติยุบตามชื่อแบบตรงตัว พิมพ์ต่างกันนิดเดียว (`MH-XTC01` vs `MH-XTC 01`) จะกลายเป็นคนละชีท
- ตัวเลือกมาจาก **2 แหล่งรวมกันเสมอ**: query `lesson_logs.sheets` ล่าสุด 500 แถว + ที่มีอยู่ในหน่วยความจำ (`sheetNameOptions()`) — ห้ามให้ผล query เขียนทับอย่างเดียว เพราะ PostgREST คืน `[]` โดยไม่มี error ได้ (เช่นตอน RLS บล็อก) แล้วตัวเลือกที่ใช้ได้จะหายหมด
- **ติดตามวิดิโอค้าง** (`pendingVideos()`) — ชีทที่**ไม่เคยติ๊กเลยสักคาบ** จะโผล่ 2 ที่: ป้าย **"⏳ วิดิโอค้าง N"** บนหัวการ์ดในแท็บบันทึก (hover ดูชื่อชีทได้) และกล่องเหลือง **"⏳ ยังดูวิดิโอไม่จบ: ..."** ในภาพสรุป
- **เวลาเข้า-เลิก** เติมเวลาของคาบมาให้ก่อน (M13 → 13:00/15:00) ครูแก้เฉพาะตอนนักเรียนมาสาย/กลับก่อน · มี validate ว่าเวลาเลิกต้องไม่ก่อนเวลาเข้า
- **ครูบันทึก 2 ทาง:** (ก) แตะไอคอน 📝 บนชิปนักเรียนในตารางเรียน (แตะที่ตัวชิปยังเปิดหน้าแก้นักเรียนเหมือนเดิม — ใช้ `stopPropagation`) (ข) แท็บ **"📘 บันทึกการเรียน"**
- แท็บบันทึกการเรียนเป็น **การ์ดพับได้ (accordion) กางได้ทีละคน** — เห็นแค่ชื่อ + จำนวนบันทึก + วันที่ล่าสุด แตะชื่อจึงกางประวัติ (มีนักเรียน ~68 คน ถ้ากางหมดจะอ่านไม่ไหว) · กดคนใหม่ คนเดิมพับเอง · ถ้ากรอง/ค้นหาจนเหลือคนเดียวจะกางให้อัตโนมัติ · สลับด้วย class ไม่ re-render ทั้งก้อน ตำแหน่ง scroll จึงไม่กระโดด
- ค้นหาด้วยชื่อ/ชีท/การบ้าน/ความเห็น + ป้าย "ล่าสุด" ที่คาบใหม่สุดของแต่ละคน
- ชิปที่มีบันทึกแล้วขีดเส้นใต้สีเขียว · ล้างจนว่างหมด (ทั้งชีท+การบ้าน+ความเห็น) แล้วกดบันทึก = ลบบันทึกคาบนั้น
- **viewer เห็นอย่างเดียว (แต่เห็นครบทุกช่อง):** กล่อง "📘 ล่าสุดที่เรียน" บนหัวการ์ดนักเรียน (query แยก `limit 1` ต่อคน จึงข้ามเดือนได้) + บรรทัดรายละเอียดใต้คาบที่มีบันทึก + จุดเขียวบนคาบนั้น
- **ความเห็นครูเป็นกล่องพื้นม่วง** (`.lg-note` บนเว็บ / กล่องเดียวกันในภาพสรุป) — เป็นส่วนที่ผู้ปกครองอยากอ่านที่สุด เดิมเป็นข้อความเทาเส้นคั่นบางๆ กลืนไปกับรายการชีท · เลือกม่วงเพราะสีอื่นถูกจองความหมายไปแล้ว (ฟ้า=เวลา · เขียว=ดูวิดิโอจบ · เหลือง=วิดิโอค้าง)
- ข้อความที่ครูพิมพ์ทั้งหมด render ผ่าน `escHtml()` (กัน XSS) และคงบรรทัดใหม่ด้วย CSS `white-space:pre-wrap` (ไม่แปลงเป็น `<br>` เพื่อไม่ให้ markup กลับเข้ามา)

### 4.8 ภาพสรุปรายเดือนส่งผู้ปกครอง (.png)
- **admin เท่านั้น** — ในแท็บ "บันทึกการเรียน" หัวการ์ดของนักเรียนแต่ละคนมีปุ่ม **"📤 ภาพสรุปเดือนนี้"** (เดือนตามที่เลือกอยู่บนแถบบน · บนมือถือเหลือแต่ไอคอน) กดได้โดยไม่ต้องกางการ์ด → เปิดพรีวิว → ดาวน์โหลด `.png`
- เนื้อหาในภาพ: หัวกระดาษ + การ์ดนักเรียน · สถิติ 3 ช่อง (เข้าเรียนกี่ครั้ง / รวมชั่วโมง / จำนวนชีท) · แถบความคืบหน้า วิดิโอที่ดูจบ + โจทย์ที่ทำ · **รายการรายวันว่าเรียนชีทอะไรไปบ้าง** พร้อมการบ้านและความเห็นครูของแต่ละคาบ
- **วาดด้วย Canvas 2D เอง ไม่พึ่งไลบรารีนอก** (เช่น html2canvas) — โปรเจกต์นี้เคยเจอ CDN ล่มบนมือถือมาแล้ว 2 ครั้ง จึงไม่เพิ่ม dependency
- ออกแบบที่ความกว้าง logical 400px แล้ว `ctx.scale(2.7)` → ไฟล์กว้าง 1080px แนวตั้ง (สูงตามเนื้อหา) ส่งไลน์ได้พอดี
- ตัดบรรทัดภาษาไทยด้วย `Intl.Segmenter('th',{granularity:'word'})` (ไทยไม่มีช่องว่างระหว่างคำ) มี fallback ตัดทีละอักขระ · ความสูงการ์ดคำนวณ 2 pass (วัดก่อน แล้วค่อยวาด)
- ระยะกันชนของป้าย "วิดิโอ/x-y หน้า" วัดจากความกว้างจริงของป้าย ไม่ใช่ค่าคงที่ ชื่อชีทยาวจึงไม่ชนป้าย

### 4.5 ลืมรหัสผ่าน
- ลิงก์ "ลืมรหัสผ่าน?" → `resetPasswordForEmail` ส่งอีเมล → คลิกลิงก์กลับมา (`PASSWORD_RECOVERY`) → ตั้งรหัสใหม่ (`updateUser`)

### 4.6 แจ้งเตือน LINE เมื่อมีคนลา
- Postgres trigger + `pg_net` ยิงตรงไป **LINE Messaging API** ตอนมีแถวใหม่ใน `leave_requests` (LINE Notify ปิดบริการแล้ว จึงใช้ Messaging API)
- ปัจจุบัน: **broadcast** หาทุกคนที่แอด OA, แจ้งทุกครั้งที่ลา

---

## 5. ไฟล์ SQL / migration (รันใน Supabase SQL Editor)

| ไฟล์ | หน้าที่ | สถานะ |
|---|---|---|
| `supabase-leave-migration.sql` | สร้าง `leave_requests` + RLS (ใช้ `student_ids`) | ✅ รันแล้ว |
| `supabase-username-migration.sql` | เพิ่มคอลัมน์ `display_name` | ✅ รันแล้ว |
| `supabase-line-notify.sql` | trigger + pg_net แจ้ง LINE (**มี token — gitignore ไว้**) | ✅ รันแล้ว |
| `supabase-lesson-log-migration.sql` | **v2** — `lesson_logs` (เวลา/ชีท jsonb/ความเห็นครู) + RLS (admin เขียน / viewer อ่าน) · อัปเกรดจาก v1 ให้เอง (ย้าย `content` → ชีทแผ่นแรก แล้ว drop) | ✅ รันแล้ว |
| `supabase-course-migration.sql` | เพิ่มคอลัมน์ `students.course` + index (ข้อมูลเดิมเป็น `'HB'` อัตโนมัติ) | ✅ รันแล้ว |
| `supabase-copy-hb-to-skill.sql` | คัดลอกรายชื่อ HB → Skill (ไม่คัดลอกตาราง) + ผูกผู้ปกครองให้เห็น Skill ด้วย · รันซ้ำได้ | ⏳ รอรัน |

> ⚠️ `supabase-line-notify.sql` มี LINE Channel Access Token → ถูก `.gitignore` ไว้ **ห้าม commit ขึ้น repo**

---

## 6. ตั้งค่า Supabase ที่ต้องมี

- **Authentication → URL Configuration** (สำหรับลืมรหัสผ่าน): Site URL + Redirect URLs = `https://sapiens091141.github.io/HB-schedule/**`
- `mailer_autoconfirm: true` (สมัครแล้วใช้ได้ทันที ไม่ต้องยืนยันอีเมล)
- Extension **`pg_net`** เปิด (สำหรับแจ้ง LINE)
- รีเซ็ตรหัสผ่านผู้ใช้ต้องทำใน Dashboard → Authentication → Users (anon key ทำไม่ได้)

---

## 7. หมายเหตุการพัฒนา (gotchas)

- **แก้จากโฟลเดอร์นี้เท่านั้น** — เป็น clone จริงของ repo (เคยมีไฟล์เก่า 51KB ค้างในเครื่องที่ไม่ตรงกับตัวจริง 87KB มาก่อน)
- ไฟล์เก็บเป็น **LF** (git `autocrlf=true` → repo เก็บ LF, checkout เป็น CRLF) — warning "LF will be replaced by CRLF" ไม่เป็นไร
- **`index.html` ไม่มี YAML front matter** + มี `.nojekyll` → GitHub Pages เสิร์ฟไฟล์ตรงๆ ไม่ผ่าน Jekyll
- Debug LINE: `select status_code, content from net._http_response order by created desc limit 5;`

---

## 8. Changelog (งานที่ทำ 2026-07-04)

| commit | งาน |
|---|---|
| `8fa286f` | ฟีเจอร์ขอลา + กติกา 2 วัน + admin อนุมัติ (บนโมเดล `student_ids`) |
| `cd3f8b6` | บังคับตั้งชื่อผู้ใช้ตอนลงทะเบียน (`display_name`) |
| `6c90016` | ลืมรหัสผ่าน / ตั้งรหัสใหม่ |
| `fa58877` | gitignore ไฟล์ LINE notify (กัน token หลุด) |
| `44c0f41` | กติกา 2 วัน สำหรับยกเลิกการลาเอง |
| `86b10bc` | เพิ่ม hint ให้หาปุ่มยกเลิกการลาเจอ |
| `8fb3632` | เพิ่ม `.nojekyll` กัน Pages build ล้ม |

## 9. Changelog (2026-08-27)

| งาน | รายละเอียด |
|---|---|
| ฟีเจอร์บันทึกการเรียน | ตาราง `lesson_logs` (รายคาบ) + modal 📝 บนชิปตารางเรียน + แท็บที่ 5 "บันทึกการเรียน" + กล่อง "ล่าสุดที่เรียน" ในหน้า viewer · ดู §4.7 |
| ขยายเป็น v2 | เพิ่มเวลาเข้า-เลิกเรียน, ชีทหลายแผ่น (ชื่อ/วิดิโอจบ/โจทย์กี่หน้า), ความเห็นจากผู้สอน · ตัดช่อง `content` ทิ้ง ใช้ชื่อชีทแทน |
| ภาพสรุปรายเดือน .png | วาดด้วย Canvas 2D เอง (ไม่พึ่ง CDN) ส่งผู้ปกครองทางไลน์ได้ · ดู §4.8 |
| แท็บบันทึกเป็นการ์ดพับได้ | กางทีละคน แก้ปัญหารายการยาวจนรก |
| แก้หน่วยให้ถูก | ชีท→"ชีท" (เดิมเขียน "แผ่น") · วิดิโอ→"คลิป" · ป้ายติ๊กเป็น "ดูวิดิโอจบแล้ว" |
| เน้นความเห็นครู | ทำเป็นกล่องพื้นม่วงมีหัวข้อในตัว ทั้งบนเว็บ (viewer + แท็บบันทึก) และในภาพสรุป .png |

## 10. Changelog (2026-09-02)

| งาน | รายละเอียด |
|---|---|
| รองรับคอร์ส Skill | เพิ่มมิติคอร์สที่ `students.course` ตารางเดียว + `const COURSES` + ปุ่มสลับคอร์สบนแถบบน · ดู §1.1 |
| กฎ union วัน/คาบ | `getClassDays()`/`visibleSlots()` รวมวัน-คาบที่มีข้อมูลจริงเข้ามาด้วย → ปรับวัน/คาบในอนาคตแล้วข้อมูลเก่าไม่หาย |
| สีคาบเป็นจานสีตามลำดับ | ลบ `.chip-M10`/`.vc-slot-M10` ฯลฯ ทิ้ง ใช้ `SLOT_PALETTE` + inline style คาบใหม่ได้สีเอง |
| viewer หลายคอร์ส | จัดกลุ่มการ์ดตามคอร์ส มีหัวข้อคั่น + ป้ายคอร์ส (แสดงเมื่อมีลูกมากกว่า 1 คอร์ส) |
| กัน import ผิดคอร์ส | อ่านชื่อคนได้แต่ไม่เจอคาบของคอร์สที่เลือกเลย → หยุดพร้อมบอกให้สลับคอร์ส |
| ผูกนักเรียน/คำขอลา ข้ามคอร์ส | ใช้ `allStudents` แทน `students` + แสดงชื่อคอร์สกำกับ |
| เปลี่ยนชื่อแอปเป็นชื่อกลาง | `manifest.json` + `<title>` เป็น "ตารางเรียน Sapiens" (เว็บนี้ครอบ 2 คอร์สแล้ว) |
| topbar จอแคบ | เพิ่ม `@media (max-width:1150px)` ซ่อนอีเมลผู้ใช้ — แถบเลือกคอร์สกินที่เพิ่ม ~95px จนเดิมล้น |
